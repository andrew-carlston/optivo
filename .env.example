# Neon Postgres (from Neon dashboard or `npx neonctl connection-string`)
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# Neon Auth is configured in the Neon dashboard — no additional env vars needed
# Auth users are stored in the neon_auth schema in your database
